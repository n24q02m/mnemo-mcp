## 2025-03-24 - N+1 Query Optimization in Entity Upserts
**Learning:** For small-to-medium datasets (e.g. graph entity extraction returning 10-200 entities), Python sqlite3 driver's looping `execute` for point reads (using a `SELECT` against a unique index) followed by a bulk `executemany` for inserts and updates is incredibly fast, even faster than constructing massive `IN (VALUES ...)` queries or relying on `executemany` UPSERTs without `RETURNING`.
**Action:** Replaced the N+1 looping `execute(SELECT)` and conditional `execute(INSERT)`/`execute(UPDATE)` statements in `graph.py`'s `upsert_entities` function with an initial loop to deduplicate unique entities and query the DB, followed by unified `conn.executemany` bulk insertions and updates. This delivered measurable ~2x improvements on inserts/updates without relying on external dependencies or risking query size limits.
## 2026-04-04 - Deferred Join Optimization for FTS5 Pagination
**Learning:** In SQLite FTS5, running a  query that simultaneously joins a full standard table (like ) results in significant N+1 evaluation overhead because the database engine may materialize the joined rows before applying the pagination limit.
**Action:** Refactored FTS search by isolating the tiered FTS subqueries to only select uid=1001(jules) gid=1001(jules) groups=1001(jules),27(sudo),103(docker) and , calculating the top  subset in an outer CTE (), and solely deferring the final  to the main table at the very end. This dramatically reduces memory and CPU overhead while preserving exact query semantics.
## 2024-04-04 - Deferred Join Optimization for FTS5 Pagination
**Learning:** In SQLite FTS5, running a `LIMIT` query that simultaneously joins a full standard table (like `memories`) results in significant N+1 evaluation overhead because the database engine may materialize the joined rows before applying the pagination limit.
**Action:** Refactored FTS search by isolating the tiered FTS subqueries to only select `id` and `tier_idx`, calculating the top `LIMIT` subset in an outer CTE (`best_tier`), and solely deferring the final `JOIN` to the main table at the very end. This dramatically reduces memory and CPU overhead while preserving exact query semantics.
